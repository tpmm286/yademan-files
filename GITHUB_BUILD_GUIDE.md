# راهنمای جامع ساخت خروجی‌های سه‌گانه برنامه «یادمان» (Web , Android APK , Windows EXE)

پروژه به طور کامل پیکربندی شده و هم بر روی **گیت‌هاب اکشن (GitHub Actions)** به صورت خودکار و ابری، و هم بر روی **کامپیوتر محلی شما** قابل ساخت و بیلد است.

---

## ۱. راهنمای بیلد روی کامپیوتر شما (Local Build Guide)

اگر می‌خواهید مستقیماً روی کامپیوتر شخصی خود با `npm` یا `bun` بیلد بگیرید:

### پیش‌نیازها:
- نصب بودن **Node.js** (نسخه 18 یا 20 یا بالاتر) یا **Bun**
- نصب پکیج‌ها با اجرای دستور زیر در ترمینال:
```bash
npm install
# یا اگر از bun استفاده می‌کنید:
bun install
```

### دستورات بیلد انواع خروجی‌ها روی کامپیوتر:

#### ۱. خروجی وب استاتیک (HTML/JS/CSS):
```bash
npm run build
# یا
bun run build
```
فایل‌های خروجی در پوشه `dist/` قرار می‌گیرند.

#### ۲. خروجی نرم‌افزار ویندوز (`.exe`):
```bash
npm run electron:build
```
فایل‌های نصب‌کننده (NSIS) و پرتابل در پوشه `release/` تولید خواهند شد.

#### ۳. خروجی اندروید (`.apk`):
برای بیلد اندروید روی کامپیوتر به **Android Studio** نیاز است:
```bash
# همگام‌سازی و آماده‌سازی فایل‌های اندروید:
npm run cap:build
# باز کردن پروژه در اندروید استودیو جهت تولید APK نهایی:
npm run cap:open
```

---

## ۲. خروجی خودکار با GitHub Actions (بدون نیاز به نصب ابزارهای سنگین روی سیستم)

ساده‌ترین راه برای دریافت فایل‌های APK و EXE بدون نیاز به نصب Android Studio یا ابزارهای ویندوز، استفاده از سرورهای ابری گیت‌هاب است:

1. پروژه را به مخزن گیت‌هاب خود Push کنید.
2. وارد تب **Actions** در بالای ریپوزیتوری شوید.
3. بیلد خودکار شروع می‌شود و پس از اتمام، از بخش **Artifacts** می‌توانید فایل‌ها را مستقیماً دانلود کنید:
   - `yadman-android-apk` (فایل `app-debug.apk` آماده نصب روی گوشی)
   - `yadman-windows-exe` (فایل `.exe` آماده اجرا در ویندوز)
   - `yadman-web-app-zip` (فایل فشرده وب‌سایت)
